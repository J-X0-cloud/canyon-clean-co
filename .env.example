# Public site URL, used for canonical links and Open Graph metadata.
NEXT_PUBLIC_SITE_URL=https://canyonclean.com

# Optional: quote requests are POSTed here as JSON (e.g. a CRM or Zapier catch hook).
# When unset, requests are logged on the server instead.
LEAD_WEBHOOK_URL=
LEAD_WEBHOOK_SECRET=
